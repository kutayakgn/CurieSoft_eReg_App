class Events {
  String title;
  String level;
  double indicatorValue;
  int price;
  String content;

  Events(
      {this.title, this.level, this.indicatorValue, this.price, this.content});
}
